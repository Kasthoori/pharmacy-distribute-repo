server:
  port: 9002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pharmacy_drug
    username: root
    password: pharmacy123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/